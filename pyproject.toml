[tool.poetry]
name = "start-hack-2024"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"
packages = [{ include = "start-hack-2024"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
